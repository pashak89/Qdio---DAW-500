# ── Entire build output and 3rdparty (keep locally, don't push) ─────────────
build2019/
daw-3-SampleEditor/3rdparty/
daw-3-SampleEditor/juce/

# Runtime logs / crash dumps
Logs/
*.dmp

# Claude harness internals
.claude/scheduled_tasks.lock
